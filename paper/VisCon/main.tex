\documentclass[10pt,twocolumn,letterpaper]{article}

\usepackage[review]{cvpr}

\input{preamble}

\definecolor{cvprblue}{rgb}{0.21,0.49,0.74}
\usepackage[pagebackref,breaklinks,colorlinks,allcolors=cvprblue]{hyperref}

\def\paperID{*****}
\def\confName{CVPR}
\def\confYear{2026}

\title{Where Do Visual Concepts Break Down? Decoder-Only Adaptation of SAM for Camouflaged Segmentation}
\author{Anonymous CVPR submission\\Paper ID \paperID}

% Patch \@maketitle to include teaser in the full-width header
\makeatletter
\let\old@maketitle\@maketitle
\renewcommand{\@maketitle}{%
  \old@maketitle
  \vspace{-0.5em}
  \begin{center}
    \includegraphics[width=0.95\textwidth]{figures/teaser.png}
    \captionof{figure}{\textbf{Decoder-only adaptation.} Base SAM poorly segments a camouflaged organism; our specialized decoder follows the true boundary because the frozen encoder already encodes the relevant texture cues. No encoder modification, no new parameters: only the 4M-parameter mask decoder is fine-tuned.}
    \label{fig:teaser}
  \end{center}
  \vspace{0.5em}
}
\makeatother

\begin{document}
\maketitle

\begin{abstract}
SAM's ViT-H encoder, pre-trained on over one billion masks, learns strong visual features, but its decoder fails to produce correct masks when objects are camouflaged. We isolate this failure by freezing the encoder and fine-tuning only the 4M-parameter mask decoder on COD10K. This raises mIoU from 57.2\% to 73.8\% and reduces prompt sensitivity from 3.5$\times$ to 1.1$\times$ across four prompt types. The decoder generalizes without retraining: +16.9 mIoU on CAMO and +17.4 on NC4K (decoder-only, center-of-mass prompts). Our findings indicate that the encoder already captures camouflage-relevant cues (texture gradients, subtle edge discontinuities), but the general-purpose decoder does not translate them into boundary predictions. Camouflage is therefore a useful diagnostic: it exposes a bottleneck in feature utilization, not feature representation.
\end{abstract}

\section{Introduction}

Foundation segmentation models learn to represent boundaries, texture transitions, and spatial layout from large-scale supervision. SAM~\cite{kirillov2023sam}, trained on over one billion masks, segments almost anything. Yet camouflaged objects expose a clear failure: on COD10K~\cite{fan2020cod10k}, SAM ViT-H achieves only 57.2\% mIoU with center-of-mass prompts and 20.5\% with edge prompts (\cref{tab:main_viscon}). Camouflaged organisms have evolved to defeat visual detection, and SAM's general-purpose decoder has never learned to read subtle texture disruptions as boundaries.

This raises a direct question: does the encoder lack the relevant features, or does the decoder fail to use them? If the ViT-H encoder, pre-trained on 11M images, already encodes the texture gradients that distinguish camouflaged foreground from background, then specializing only the decoder should recover most of the gap. That is what we observe.

Prior SAM adaptation methods typically modify the encoder via adapters~\cite{chen2023samadapter}, redesign prompt pathways~\cite{hao2024comprompter}, or add dual-stream modules~\cite{liu2025dualstream}. These approaches improve performance but conflate two questions: whether the encoder needs better features, and whether the decoder needs better feature utilization. By changing only the decoder, we isolate the latter.

The more telling result is how behavior changes across prompts. Decoder specialization sharply reduces prompt sensitivity (\cref{tab:main_viscon}), and an ablation shows this is learned: mixed point/box training consistently outperforms point-only and box-only alternatives (\cref{tab:prompt_ablation_viscon}). The specialized decoder also generalizes to held-out camouflage datasets (CAMO, NC4K) without retraining, which suggests transferable camouflage-detection behavior rather than dataset-specific shortcuts.

\noindent\textbf{Contributions.}
\begin{enumerate}
    \item We show that decoder-only adaptation yields +16.6 mIoU on COD10K with no architecture changes, 90-minute training, and a 16MB checkpoint---evidence that SAM's encoder already represents camouflage-relevant visual concepts.
    \item We demonstrate prompt robustness: mixed-prompt training reduces sensitivity from 3.5$\times$ to 1.1$\times$, and the gain transfers to held-out datasets (+16.9 mIoU on CAMO, +17.4 on NC4K).
    \item We frame camouflage as a diagnostic that separates feature representation (encoder) from feature utilization (decoder) in foundation segmentation models. The same lens may apply to other low-contrast domains such as medical imaging and underwater scenes.
\end{enumerate}

\section{Related Work}

\noindent\textbf{Camouflaged object detection.}
Dedicated COD architectures (SINet~\cite{fan2020cod10k}, ZoomNet~\cite{pang2022zoomnet}, SegMaR~\cite{jia2022segmar}) design task-specific networks from scratch. We instead leverage SAM's pre-trained representations and ask how far decoder-only adaptation can go without domain-specific architecture.

\noindent\textbf{SAM adaptation.}
SAM-Adapter~\cite{chen2023samadapter} injects learnable modules inside the encoder, COMPrompter~\cite{hao2024comprompter} redesigns the prompt pathway, and Liu~\etal~\cite{liu2025dualstream} add dual-stream adapters. Each of these modifies the encoder, which conflates feature quality with feature utilization. We fine-tune only the existing decoder, isolating the utilization question.

\noindent\textbf{Parameter-efficient fine-tuning.}
LoRA~\cite{hu2022lora} and prompt tuning~\cite{lester2021prompt} adapt large models by adding small parameter sets to the encoder or prompt pathway. Our approach is simpler: we add zero new parameters and train only the existing 4M-parameter mask decoder.

\section{Method}
SAM consists of a ViT-H image encoder ($\sim$632M parameters), a prompt encoder, and a mask decoder ($\sim$4M parameters). We freeze the image encoder and prompt encoder, and train only the mask decoder (\cref{fig:architecture}).

\noindent\textbf{Pre-computed embeddings.} We run the frozen encoder once on all training images and cache embeddings as \texttt{.npy} files, eliminating the encoder forward pass from training.

\noindent\textbf{Mixed-prompt training.} Each iteration randomly selects between a point prompt (random foreground pixel) and a bounding box prompt (tight GT box) with 50/50 probability. This guards against the decoder overfitting to one prompt type and gives us a direct test of whether gains transfer across prompt styles.

\noindent\textbf{Loss and optimization.} We minimize $\mathcal{L} = 0.5 \cdot \mathcal{L}_{\text{BCE}} + 0.5 \cdot \mathcal{L}_{\text{Dice}}$, where BCE enforces pixel-level boundaries and Dice maintains region-level coherence. Ablations over four weighting variants (BCE-only, Dice-only, 0.7/0.3, 0.3/0.7) show all fall within $\pm$2.1 mIoU of the 0.5/0.5 baseline; the choice is not sensitive. We use AdamW (lr $10^{-4}$, cosine schedule, 1-epoch warmup, 15 epochs). Training takes $\sim$90 minutes on a single A100 with pre-computed embeddings.

\noindent\textbf{Evaluation protocol.} We evaluate four prompt strategies of increasing difficulty: \textbf{Center-of-Mass} (1pt), \textbf{Edge} (1pt on boundary contour), \textbf{Multi-Grid} (4pt), and \textbf{Multi-Random} (3pt). Following SAM's standard protocol~\cite{kirillov2023sam}, prompts are derived from ground-truth masks to simulate idealized user clicks. We report mIoU on the full 2,026-image COD10K camouflaged test set, as well as on CAMO (250 images) and NC4K (4,121 images) for cross-dataset evaluation.

\begin{figure}[t]
    \centering
    \includegraphics[width=\columnwidth]{figures/architecture_diagram.pdf}
    \caption{\textbf{SAM adaptation pipelines.} (a)~Decoder-only training: we freeze the encoder, cache embeddings, and fine-tune only the 4M-parameter mask decoder. (b)~Optional LLPM + decoder training: a small preprocessing module sharpens input features before the frozen encoder. (c)~Inference with either configuration.}
    \label{fig:architecture}
\end{figure}

\section{Results}

\subsection{Decoder Adaptation Improves All Prompt Types}

\begin{table}[t]
\centering
\caption{\textbf{Main results on COD10K} (2,026 test images). Decoder-only adaptation improves all prompt strategies and sharply reduces prompt sensitivity.}
\label{tab:main_viscon}
\small
\begin{tabular}{l cc cc c}
\toprule
 & \multicolumn{2}{c}{Base SAM} & \multicolumn{2}{c}{Ours} & \\
\cmidrule(lr){2-3} \cmidrule(lr){4-5}
Prompt & mIoU & $S_\alpha$ & mIoU & $S_\alpha$ & $\Delta$ mIoU \\
\midrule
Center-of-Mass & .572 & .743 & .738 & .847 & +.166 \\
Edge & .205 & .481 & .694 & .810 & +.489 \\
Multi-Grid & .682 & .812 & .756 & .861 & +.074 \\
Multi-Random & .727 & .830 & .776 & .874 & +.049 \\
\midrule
\textit{Range} & \multicolumn{2}{c}{\textit{3.5$\times$}} & \multicolumn{2}{c}{\textit{1.1$\times$}} & \\
\bottomrule
\end{tabular}
\end{table}

\Cref{tab:main_viscon} shows that freezing the encoder and adapting only the decoder improves every prompt strategy. The largest gain is on edge prompts (+238\% relative), the setting where subtle boundary interpretation matters most. Base SAM's mIoU varies 3.5$\times$ across prompt types (0.205 to 0.727); the specialized decoder varies only 1.1$\times$ (0.694 to 0.776). Put differently: the visual information needed for camouflage segmentation is already in the frozen representation; the default decoder simply does not convert it into boundary decisions.

\subsection{Per-Category Analysis}

\begin{table}[t]
\centering
\caption{\textbf{Per-category mIoU} (Center-of-Mass prompt, COD10K). Terrestrial animals---the hardest category due to strong background-matching---show the largest gain.}
\label{tab:category}
\small
\begin{tabular}{lcccr}
\toprule
Category & $N$ & Base & Ours & Rel.~Gain \\
\midrule
Amphibian & 124 & .680 & .825 & +21.4\% \\
Aquatic & 474 & .563 & .720 & +27.9\% \\
Flying & 714 & .610 & .767 & +25.6\% \\
Terrestrial & 699 & .519 & .703 & \textbf{+35.5\%} \\
\bottomrule
\end{tabular}
\end{table}

\Cref{tab:category} breaks down decoder gains by animal superclass. Terrestrial animals (insects on bark, lizards on rocks) gain the most (+35.5\%), consistent with their strong background-matching camouflage. The pattern holds across all four categories: the harder the camouflage, the larger the decoder-only gain.

\subsection{Prompt Robustness Is Learned, Not Accidental}

\begin{table}[t]
\centering
\caption{\textbf{Prompt-mix ablation} (COD10K test set). Mixed training produces the most robust decoder; box-only collapses on point prompts.\textsuperscript{$\ddagger$}}
\label{tab:prompt_ablation_viscon}
\small
\begin{tabular}{lcccc}
\toprule
\multirow{2}{*}{Training} & \multicolumn{4}{c}{Test Prompt mIoU} \\
\cmidrule(lr){2-5}
 & CoM & Edge & Grid & Random \\
\midrule
Point-only & .729 & .673 & .747 & .766 \\
Box-only & .385 & .523 & .424 & .491 \\
\textbf{Mixed (ours)} & \textbf{.738} & \textbf{.694} & \textbf{.756} & \textbf{.776} \\
\bottomrule
\end{tabular}
\vspace{1pt}
{\footnotesize \textsuperscript{$\ddagger$}Point-only and box-only evaluated on all matched COD10K test pairs ($\sim$4k); mixed row from the 2,026-image camouflaged subset.}
\end{table}

\Cref{tab:prompt_ablation_viscon} shows that prompt robustness comes from mixed training, not from fine-tuning alone. Mixed-prompt training outperforms point-only on all test strategies (by up to $\sim$2 mIoU) and box-only by 17--35 mIoU depending on the test prompt. Box-only training collapses on point prompts (0.385 CoM), which confirms that robustness requires exposure to multiple prompt modalities during training. By alternating point and box prompts, the decoder learns \emph{what} the camouflaged object is rather than \emph{where the user clicked}, grounding the mapping in object appearance rather than prompt geometry.

\subsection{Cross-Dataset Generalization}

\begin{table}[t]
\centering
\caption{\textbf{Cross-dataset generalization} (mIoU, all prompt types). Trained on COD10K only; no retraining on target datasets. \textit{Range} is max/min mIoU across prompts. +LLPM adds the 31K-parameter preprocessing module.}
\label{tab:crossdataset}
\small
\resizebox{\columnwidth}{!}{%
\begin{tabular}{llccccc}
\toprule
Dataset & Model & CoM & Edge & Grid & Rand. & Range \\
\midrule
\multirow{3}{*}{CAMO (250)} & Base SAM & .489 & .153 & .688 & .692 & 4.5$\times$ \\
 & Ours & .658 & .619 & .698 & .703 & 1.1$\times$ \\
 & +LLPM & \textbf{.677} & \textbf{.656} & \textbf{.722} & \textbf{.737} & 1.1$\times$ \\
\midrule
\multirow{3}{*}{NC4K (4,121)} & Base SAM & .567 & .127 & .726 & .739 & 5.8$\times$ \\
 & Ours & .741 & .715 & .770 & .780 & 1.1$\times$ \\
 & +LLPM & \textbf{.752} & \textbf{.742} & \textbf{.779} & \textbf{.786} & 1.1$\times$ \\
\bottomrule
\end{tabular}}
\end{table}

The real test is whether the decoder has learned generalizable behavior or memorized COD10K. \Cref{tab:crossdataset} shows it transfers: +16.9 mIoU on CAMO (center-of-mass) and +17.4 on NC4K, with no retraining. Prompt robustness also transfers, with sensitivity dropping from 4.5$\times$ to 1.1$\times$ on CAMO and from 5.8$\times$ to 1.1$\times$ on NC4K. Edge prompts, the hardest setting, show the largest absolute gains on both datasets (+46.6 on CAMO, +58.8 on NC4K), the same pattern as on COD10K. The LLPM adds +2--4 mIoU across all prompts and datasets, again largest on edge prompts (+3.7 on CAMO, +2.7 on NC4K). Both the decoder and the LLPM generalize; neither has simply memorized COD10K-specific shortcuts.

\subsection{Non-Camouflage Sanity Check}

\begin{figure}[t]
    \centering
    \includegraphics[width=\columnwidth,height=8cm,keepaspectratio]{figures/coco_comparison.png}
    \caption{\textbf{Regular-scene sanity check.} COCO val2017 images with a fixed image-center prompt (no GT). The specialized decoder produces comparable masks, confirming that camouflage training does not degrade general segmentation.}
    \label{fig:coco}
\end{figure}

Does camouflage-specific decoder training hurt performance on ordinary scenes? \Cref{fig:coco} shows COCO val2017 results using a fixed image-center prompt (no ground truth). The specialized decoder produces masks comparable to, and occasionally tighter than, base SAM. Camouflage training has not degraded general segmentation; if anything, it sharpens boundary interpretation across the board.

\subsection{Context: Comparison with Existing Methods}

\begin{table}[t]
\centering
\caption{\textbf{Landscape comparison} on COD10K. Dedicated COD methods are fully automatic; SAM-based methods use encoder/prompt tuning; ours uses oracle prompts.\textsuperscript{$\dagger$} These are \emph{not} head-to-head comparisons.}
\label{tab:comparison}
\small
\resizebox{\columnwidth}{!}{%
\begin{tabular}{llcccc}
\toprule
Method & Type & $S_\alpha$ & $E_\phi$ & $F_\beta^w$ & MAE$\downarrow$ \\
\midrule
SINet~\cite{fan2020cod10k} & Auto & .776 & .864 & .631 & .043 \\
ZoomNet~\cite{pang2022zoomnet} & Auto & .838 & .911 & .729 & .029 \\
SAM-Adpt.~\cite{chen2023samadapter} & Enc.\ adapt & .883 & .918 & .801 & .025 \\
COMPr.~\cite{hao2024comprompter} & Prompt adapt & .889 & .949 & .821 & .023 \\
\midrule
\textbf{Ours} (Grid)\textsuperscript{$\dagger$} & Dec.\ only & .861 & .951 & .841 & .024 \\
\bottomrule
\end{tabular}}
\vspace{1pt}
{\footnotesize \textsuperscript{$\dagger$}Oracle prompts from GT masks. Not comparable to fully automatic methods.}
\end{table}

\Cref{tab:comparison} contextualizes decoder-only adaptation within the broader COD landscape. Under oracle prompts, our method achieves competitive $E_\phi$ and $F_\beta^w$ but lower $S_\alpha$ than encoder-adaptation methods (SAM-Adapter, COMPrompter). This fits our overall conclusion: encoder and decoder specialization address complementary aspects of the problem. Decoder-only adaptation recovers much of the gap with no new parameters and 90-minute training, but it does not replace encoder adaptation.

\begin{figure*}[t]
    \centering
    \includegraphics[width=\textwidth,height=0.88\textheight,keepaspectratio]{figures/qualitative_comparison.png}
    \caption{\textbf{Qualitative comparison} (center-of-mass prompts, diverse difficulty). Columns: input with prompt point, GT boundary (green), base SAM prediction (with GT contour overlaid), and our decoder-only prediction (with GT contour). IoU scores appear per prediction. Examples span hard, medium, and easy cases; our decoder improves across the full spectrum.}
    \label{fig:qualitative}
\end{figure*}

\section{Discussion}

\noindent\textbf{What this reveals about visual concept representation.}
The ViT-H encoder, having seen 1B+ masks during pre-training, appears to already encode texture gradients and edge patterns that matter for camouflage. The decoder, trained on general segmentation, has not learned that \emph{subtle} texture disruptions signify boundaries. A 16.6-point mIoU gain from training only 0.6\% of parameters (\cref{tab:main_viscon}) puts the bottleneck squarely in feature utilization (decoder), not feature representation (encoder). Camouflage is useful as a diagnostic because it isolates a regime where the gap between encoding a feature and acting on it is wide.

\noindent\textbf{Prompt robustness as concept grounding.}
The sharp drop in prompt sensitivity (\cref{tab:main_viscon}) suggests that mixed-prompt training produces a decoder that reasons about object appearance rather than prompt-specific heuristics. Point-only training overfits to spatial proximity; box-only training overfits to box geometry (\cref{tab:prompt_ablation_viscon}). Mixed training forces the decoder to recover object structure, which is why improvement transfers to prompt types never seen during training.

\noindent\textbf{Where the decoder still fails.}
Analyzing the bottom quartile of predictions (400 worst images) reveals structured failure modes, not random noise. Disjoint objects (multiple connected components) account for 44\% of failures; dark images (brightness $<$ 80) account for 26\%. Tiny objects ($<$1\% of image area) are the hardest category, with a mean IoU of 0.29. These patterns point to single-point prompting and fixed-resolution decoding as the remaining bottlenecks, not missing encoder features.

\noindent\textbf{Beyond camouflage: a diagnostic lens.}
The decoder-only protocol is not specific to camouflage. Any domain where a foundation model underperforms can be tested the same way: freeze the encoder, fine-tune only the decoder, measure recovery. If most of the gap closes, the bottleneck is downstream; if not, the encoder itself needs adaptation. Medical imaging and underwater scenes are obvious next candidates, since both involve low-contrast boundaries that may already be encoded but underutilized. The per-category results (\cref{tab:category}) show the diagnostic is granular enough to reveal which \emph{types} of visual difficulty benefit most.

\noindent\textbf{Complementary directions.}
A Learnable Local Preprocessing Module (LLPM, $\sim$31K parameters) placed before the frozen encoder (\cref{fig:architecture}b--c) yields +2.5 mIoU on edge prompts on COD10K and transfers to held-out datasets (\cref{tab:crossdataset}). Encoder-adaptation methods~\cite{chen2023samadapter,hao2024comprompter} still achieve higher absolute $S_\alpha$, so encoder and decoder specialization address different parts of the problem. An adapted encoder feeding a specialized decoder, paired with an automatic prompt generator, is the obvious next step toward fully automatic deployment.

\noindent\textbf{Limitations.}
(1)~Our evaluation uses oracle GT-derived prompts following SAM's standard protocol; deployment requires a prompt source.
(2)~Results are from single training runs without confidence intervals.
(3)~Cross-dataset evaluation covers camouflage only; we have not tested non-biological camouflage (e.g., military) or the medical/underwater domains suggested above.

\section{Conclusion}
Freezing SAM's encoder and fine-tuning only its 4M-parameter decoder raises mIoU by 16.6 points on COD10K, generalizes to held-out datasets, and reduces prompt sensitivity from 3.5$\times$ to 1.1$\times$. The gap between base SAM and this simple adaptation suggests that, for camouflage at least, the encoder already knows more than the decoder can use.

{\small
\bibliographystyle{ieeenat_fullname}
\bibliography{main}
}

\end{document}

# Training configuration for Specialized SAM decoder fine-tuning

seed: 42

model:
  type: vit_h
  checkpoint: weights/sam_vit_h_4b8939.pth
  weights_url: https://dl.fbaipublicfiles.com/segment_anything/sam_vit_h_4b8939.pth

data:
  dataset: cod10k
  train_img_dir: data/cod10k/COD10K-v3/Train/Image
  train_mask_dir: data/cod10k/COD10K-v3/Train/GT_Object
  kaggle_dataset: aarekaz/cod10k

preprocessing:
  target_size: 1024
  augmentation:
    horizontal_flip: true
    rotation: false       # set true for rotation augmentation
    max_rotation: 5.0     # degrees
    pixel_shift: false    # set true for translation augmentation
    max_shift: 16         # pixels (half of ViT's 32px patch)

embeddings:
  dir: data/embeddings/camo_embeddings_vith
  metadata_csv: data/embeddings/camo_train_meta_vith.csv

training:
  epochs: 15
  learning_rate: 1.0e-4
  optimizer: AdamW
  batch_size: 1
  weight_decay: 0.01
  lr_scheduler: cosine  # cosine annealing for better convergence
  warmup_epochs: 1
  loss:
    bce_weight: 0.5
    dice_weight: 0.5
  prompt_type: random  # random switch between point and box prompts
  # save_every_n_epochs: 5  # uncomment for epoch ablation (saves at 5, 10, 15)

output:
  decoder_path: checkpoints/camo_decoder_vith.pth

huggingface:
  repo_id: AAREKAZ/SpecialSAM
  upload_embeddings: true
  upload_metadata: true
  upload_model: true
